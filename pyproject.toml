[project]
name = "agent-1"
version = "0.1.0"
description = "A replication of FDM-1"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
